#logs.tab-pane
    .panel.panel-default
        .panel-heading
            h2.panel-title 取得した位置情報
        .panel-body
            .table-responsive
                table.table.table-striped.table-hover.table-condensed
                    thead
                        tr
                            th #
                            th 取得日時
                            th
                    tbody
                        tr(v-for="x in logs")
                            td {{ x.index }}
                            td {{ displayDatetime(x.createdAt) }}
                            td
                                .btn-group
                                    button.btn.btn-primary(@click="redraw(x.lat, x.lon)") 開く
                                    button.btn.btn-default(@click="deleteLog(x.index)") 消す